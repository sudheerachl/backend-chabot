services:
  - type: web
    name: flask-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
    plan: free
    pythonVersion: "3.10"  # Specify the version
